import { Orderitemdetails } from './orderitemdetails.model';

describe('Orderitemdetails', () => {
  it('should create an instance', () => {
    expect(new Orderitemdetails()).toBeTruthy();
  });
});
